/* an array type and an array variable */
let
	type  arrtype = array of int
	var arr1:arrtype := arrtype [10] of 0
	var i := 0
	function a(i:int, j:string):int =
	    if c then 1
	    else 0
in
	while i < 3 do
		i := "\10\"nihao\"\t\t\t\'shijie\'\n"; 
	c = a(j, i);
	for i := 0 to N do (
	    break; 
	    i := i | i & i = i + i * i / -i.i
	)
end
